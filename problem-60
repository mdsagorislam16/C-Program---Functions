#include <stdio.h>

long long power(int b, int p) {
    if(p == 0) return 1;
    return b * power(b, p - 1);
}

int main() {
    int b, p;
    scanf("%d %d", &b, &p);

    printf("Result = %lld\n", power(b, p));

    return 0;
}

/*
Input:
2 10

Output:
Result = 1024
*/
