#include <stdio.h>

int isPrime(int n) {
    int i;
    if (n <= 1) return 0;

    for(i = 2; i * i <= n; i++) {
        if(n % i == 0) return 0;
    }

    return 1;
}

int main() {
    int low, high, i;
    scanf("%d %d", &low, &high);

    for(i = low; i <= high; i++) {
        if(isPrime(i)) {
            printf("%d ", i);
        }
    }

    return 0;
}

/*
Input:
10 30

Output:
11 13 17 19 23 29
*/
